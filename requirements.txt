# --- CORE AI FRAMEWORKS ---
crewai>=0.86.0
langgraph>=0.2.50
langchain>=0.3.10
langchain-groq>=0.2.3
langchain-community>=0.3.10
groq>=0.13.0
google-generativeai>=0.8.3

# --- TTS BACKENDS ---
# Chatterbox: near-human quality, MIT license, beats ElevenLabs in blind tests
# emotion control via exps parameter (0.0-2.0), zero-shot voice cloning
chatterbox-tts>=0.1.0

# FIX: pin torch to CPU-only wheel — avoids 2GB+ CUDA download on GitHub Actions
# and prevents pandas version backtracking caused by gradio's loose pandas>=1.0 bound
torch>=2.0.0,<3.0.0 --index-url https://download.pytorch.org/whl/cpu
torchaudio>=2.0.0,<3.0.0 --index-url https://download.pytorch.org/whl/cpu

# FIX: pin pandas to a modern pre-built wheel — prevents pip from backtracking
# to pandas 1.4.4 (source-only, fails to build on Python 3.11 without pkg_resources)
pandas>=2.2.2,<3.0.0

# Kokoro: fast, Apache 2.0, good fallback
kokoro-onnx>=0.4.0
soundfile>=0.12.1

# edge-tts: Microsoft Neural voices
edge-tts>=6.1.12

# gTTS: last-resort fallback only
gtts>=2.5.1
mutagen>=1.47.0

# --- VIDEO / AUDIO ---
ffmpeg-python>=0.2.0
Pillow>=11.0.0
pydub>=0.25.1

# --- HTTP / SCRAPING ---
requests>=2.31.0
aiohttp>=3.10.0
aiofiles>=23.2.0
beautifulsoup4>=4.12.0
playwright>=1.49.0
praw>=7.7.0

# --- GOOGLE / YOUTUBE ---
google-api-python-client>=2.150.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# --- DB / CONFIG ---
supabase>=2.10.0
pydantic>=2.9.0,<3.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.0

# --- UTILITY / OBSERVABILITY ---
tenacity>=8.4.0
loguru>=0.7.0
httpx>=0.27.0
langfuse>=2.57.2

# --- TESTING ---
pytest>=8.3.0
