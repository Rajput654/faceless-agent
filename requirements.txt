# ============================================================
# PINNED VERSIONS — prevents pip resolution-too-deep errors
# All versions verified compatible with each other.
# ============================================================

# --- CORE AI FRAMEWORKS ---
crewai==1.6.1
langgraph==1.0.5
langgraph-checkpoint==2.1.2
langgraph-prebuilt==1.0.8
langgraph-sdk==0.3.12
langchain==1.2.3
langchain-core==1.2.23
langchain-groq==0.2.3
langchain-community==0.3.10
langchain-classic==1.0.2
langchain-text-splitters==1.1.1
groq==0.37.0
google-generativeai==0.8.5

# --- TTS BACKENDS ---
chatterbox-tts==0.1.7
torch==2.6.0
torchaudio==2.6.0

# pre-install numpy before pip resolves source packages
numpy>=1.26.0,<3.0.0

# prevents backtrack to pandas 1.4.4 (no Py3.11 wheel)
pandas>=2.2.2,<3.0.0

# Kokoro: fast, Apache 2.0, good fallback
kokoro-onnx==0.4.9
soundfile>=0.12.1

# edge-tts: Microsoft Neural voices
edge-tts>=6.1.12

# gTTS: last-resort fallback only
gtts>=2.5.1
mutagen>=1.47.0

# --- VIDEO / AUDIO ---
ffmpeg-python>=0.2.0
Pillow>=11.0.0
pydub>=0.25.1

# --- HTTP / SCRAPING ---
requests>=2.31.0
aiohttp>=3.10.0
aiofiles>=23.2.0
beautifulsoup4>=4.12.0
playwright>=1.49.0
praw>=7.7.0

# --- GOOGLE / YOUTUBE ---
google-api-python-client>=2.150.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# --- DB / CONFIG ---
supabase>=2.10.0
pydantic>=2.9.0,<3.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.0

# --- UTILITY / OBSERVABILITY ---
tenacity>=8.4.0
loguru>=0.7.0
httpx>=0.27.0
langfuse>=2.57.2

# --- TESTING ---
pytest>=8.3.0
